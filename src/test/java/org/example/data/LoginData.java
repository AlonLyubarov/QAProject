package org.example.data;

// Class representing a single login record
public class LoginData {
    public String email;       // user email
    public String password;    // user password
    public boolean expected;   // expected result: true if login should succeed, false if fail
}